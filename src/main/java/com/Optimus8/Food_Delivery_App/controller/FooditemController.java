package com.Optimus8.Food_Delivery_App.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/food")
public class FooditemController {
     // get all food for a particular category
    // get all MAIN_COURSE item  whit prices above  x reupee for a particular

    // get all veg food of restaurant

    // get all non veg food of a restaurant

    // get cheapest 5 food items of a particular  restaurant
    // get costly 5 food items of a particular  restaurant
    // get costly 5 food items of a particular  category
    // get cheapest 5 food items of a particular  restaurant -> name of dish and name of  restaurant
}
